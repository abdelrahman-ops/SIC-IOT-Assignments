const fs = require('fs');
const path = require('path');
const { encodeImage, decodeImage } = require('./util'); // Replace with your utility functions for image encoding/decoding

module.exports = { encodeImage, decodeImage };
